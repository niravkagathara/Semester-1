//Print average of three numbers. (with & without scanf)
#include <stdio.h>

int main()
{
    int a = 4, b = 5, c = 6, avg;

    avg = ((a + b + c) / 3);
    printf("avg is %d", avg);

    return 0;
}
