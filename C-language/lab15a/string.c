//Input a string in character array and print string and length of string.
#include<stdio.h>

int main(){
	int n, i, len=0;
	scanf("%d", &n);
	
	char arr[n];
	printf("enter string :");
	scanf("%c",&arr[n]);
}